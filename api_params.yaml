environments:
  - name: dev
    endpoints:
      production:
	url: https://reqres.in/
	config:
            retryTimeOut: $RETRY
      sandbox:
	url: https://google.com
	config:
            retryTimeOut: $RETRY
  - name: production
    endpoints:
      production:
	url: https://reqres.in/
	config:
            retryTimeOut: 
      sandbox:
	url: https://reqres.in/
	config:
            retryTimeOut: $RETRY
